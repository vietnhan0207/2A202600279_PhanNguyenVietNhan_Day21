# Báo cáo MLOps Lab - Phan Nguyễn Việt Nhân

**Link GitHub Repository:** `[Điền link GitHub của bạn vào đây]`

## 1. Kết quả MLflow (Giai đoạn 1)
*(Chèn ảnh chụp màn hình MLflow UI hiển thị ít nhất 3 thí nghiệm với các siêu tham số khác nhau vào đây)*

## 2. Kết quả CI/CD Pipeline (Giai đoạn 2 & 3)
*(Chèn ảnh chụp màn hình tab GitHub Actions hiển thị luồng Unit Test -> Train -> Eval -> Deploy đều tích xanh)*

## 3. Hoạt động của API trên VM
Dưới đây là kết quả kiểm tra Endpoint trên máy chủ thật:

**(Kết quả lệnh `/health`)**
*(Chèn ảnh chụp màn hình hoặc kết quả của lệnh `curl http://34.136.245.52:8000/health`)*

**(Kết quả lệnh `/predict`)**
*(Chèn ảnh chụp màn hình chạy thử một request POST dự đoán lên API)*

## 4. Dữ liệu trên Google Cloud Storage
*(Chèn ảnh chụp màn hình giao diện web Google Cloud Storage Console cho thấy các file `.csv` và `model.pkl` đã được tải lên)*
